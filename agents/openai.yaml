interface:
  display_name: "Prose Polish RU"
  short_description: "Режет воду, хуманизирует, называет AI-маркеры"
  default_prompt: "Use $prose-polish-ru to polish and humanize this Russian draft. Preserve facts, links, and code. Name AI markers, water, and bad signs."
policy:
  allow_implicit_invocation: false
