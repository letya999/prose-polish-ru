interface:
  display_name: "Prose Polish RU"
  short_description: "Редактор и жёсткий хуманизатор русского текста"
  default_prompt: "Use $prose-polish-ru. Route the request: write/edit/humanize → do the work; evaluate → audit; unnamed → score card. Preserve protected spans. On article/post apply Pass H (slips, roughness, stance)."
policy:
  allow_implicit_invocation: false
